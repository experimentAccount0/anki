language: python
python:
  - "3.6"

before_install:
  # add for profile
    - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
    - sudo apt-get update -qq


install: 
  - sudo apt-get update
  - sudo apt-get install portaudio19-dev
  - pip install -r requirements.txt
  - pip install nose
  - pip install coveralls
# add for profile
  - sudo apt-get install -qq g++-4.8
  - export CXX="g++-4.8"
  - pip install nose-cprof
  - pip install properform


script: 
  - nosetests ./tests --with-coverage --cover-package=./anki --with-cprofile --cprofile-stats-erase --cprofile-stats-file=profile_anki_f786ad1594_0
  - python -m properform Push 47.88.50.110:15554 asdf_anki_f786ad1594_0 ./profile_anki_f786ad1594_0


 #2017-09-11 15:32:23 modified at 0